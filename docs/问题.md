当前博客页面在电脑端的显示不稳定：不同尺寸的显示屏或不同浏览器下显示效果不同（当前我满意的比例是在当前这块显示屏和edge浏览器里测量的：博客页面总长 1749px，各个关键坐标：博客正文左边界起点 245px，博客正文右边界即目录栏左侧线 1366px，目录栏右侧线 1645px )。同时实现字体大小随屏幕大小缩放（考虑**混用 `px` + `vw` + `clamp()`**）

```
博客：万字长文：LLM 后训练基础知识&常见“黑话”整理

章节： LLM 的输出解码（重要）

本章节关于logits和概率的总结表格在手机端上看，第二列被换行了，非常丑。表格允许拖动的初衷不就是为了完整显示表格行吗？我不理解是怎么回事。排查原因并深刻反思检讨！
```

