2026.9.9 2:32 分，发现 隐藏的网站内容又可以看到了（测试环境：inprivate 和 未登陆过管理后台的电脑上都能看到了）

---

**已修复(2026-09-09,commit 待推送时见 git log)**

原因:不是缓存。凌晨 2:32 你的代理节点连不上 workers.dev(那晚它一直时好时坏),你自己的浏览器查不到即时通道,只能看仓库开关文件;而那份文件还是「开放」——因为你点「隐藏」是在旧版界面(v1,只写 KV 不写文件),v3 上线后没有重新点过。所以:能连上 workers.dev 的设备一直锁着,连不上的设备(含 2:32 的你 + 全部国内直连访客)靠文件通道,文件说开放就没锁。

修复:
1. 仓库开关文件拨到「隐藏」,与 KV 对齐(两通道恢复一致);
2. 后台「站点开关」页现在分两条通道显示状态,不一致会黄条警告,按提示完成一整次开关切换即自动对齐;
3. 新增 `.githooks/pre-push` 拦截:本地把「隐藏」的开关文件改回「开放」再推送会被拦下(强制覆盖用 `GIT_ALLOW_SITE_UNLOCK=1 git push`),防止以后本地没 pull 就 push 把锁冲掉。
